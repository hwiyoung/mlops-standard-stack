-- ============================================
-- pgstac 초기화 및 STAC 컬렉션 생성
-- ============================================

-- pgstac 확장 설치 (pypgstac migrate로 수행)
-- 이 파일은 초기 컬렉션 생성을 위한 스크립트

-- 참고: pgstac은 stac-fastapi가 시작할 때 자동으로 스키마를 생성합니다.
-- 이 스크립트는 컬렉션 초기화를 위해 사용됩니다.

-- 드론 사진 컬렉션
-- (stac-fastapi 시작 후 API를 통해 생성)

-- 정사영상 컬렉션  
-- (stac-fastapi 시작 후 API를 통해 생성)
